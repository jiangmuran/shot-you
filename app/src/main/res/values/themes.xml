<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat (DayNight) base so AppCompatActivity + per-app locales work; the real
         look comes from the Compose Material 3 theme. -->
    <style name="Theme.ShotYou" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
    </style>

    <style name="Theme.ShotYou.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ShotYou</item>
    </style>
</resources>
