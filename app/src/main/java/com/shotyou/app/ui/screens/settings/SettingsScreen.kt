package com.shotyou.app.ui.screens.settings

import androidx.compose.runtime.Composable
import com.shotyou.app.ui.components.StubScreen

/** P1 stub — owned by the Settings/Templates agent in P2. */
@Composable
fun SettingsScreen() {
    StubScreen(title = "Settings", subtitle = "Providers, API keys, queue & quota")
}
