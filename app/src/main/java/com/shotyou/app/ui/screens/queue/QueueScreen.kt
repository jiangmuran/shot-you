package com.shotyou.app.ui.screens.queue

import androidx.compose.runtime.Composable
import com.shotyou.app.ui.components.StubScreen

/** P1 stub — owned by the Queue agent in P2. */
@Composable
fun QueueScreen(onOpenResult: (String) -> Unit) {
    StubScreen(title = "Queue", subtitle = "Background generation jobs")
}
