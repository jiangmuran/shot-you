package com.shotyou.app.ui.screens.library

import androidx.compose.runtime.Composable
import com.shotyou.app.ui.components.StubScreen

/** P1 stub — owned by the Photo/Library agent in P2. */
@Composable
fun LibraryScreen(onPhotosGrouped: () -> Unit) {
    StubScreen(title = "Library", subtitle = "Pick photos and group them with AI")
}
