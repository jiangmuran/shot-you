package com.shotyou.app.ui.screens.groups

import androidx.compose.runtime.Composable
import com.shotyou.app.ui.components.StubScreen

/** P1 stub — owned by the Review/Generate agent in P2. */
@Composable
fun GroupsScreen(onOpenGroup: () -> Unit, onBack: () -> Unit) {
    StubScreen(title = "Groups", subtitle = "AI-grouped near-duplicate shots")
}
