package com.shotyou.app.ui.screens.usage

import androidx.compose.runtime.Composable
import com.shotyou.app.ui.components.StubScreen

/** P1 stub — owned by the Queue/Usage agent in P2. */
@Composable
fun UsageScreen() {
    StubScreen(title = "Usage", subtitle = "API call counts & estimated cost")
}
